easyblock = 'ConfigureMake'

name = 'c-ares'
version = '1.34.6'

homepage = 'https://c-ares.org/'
description = "c-ares is a C library for asynchronous DNS requests (including name resolves)"

toolchain = {'name': 'GCCcore', 'version': '12.3.0'}
toolchainopts = {'pic': True}

github_account = 'c-ares'
source_urls = ['https://github.com/c-ares/c-ares/releases/download/v%(version)s']
sources = ['%(name)s-%(version)s.tar.gz']
checksums = ['912dd7cc3b3e8a79c52fd7fb9c0f4ecf0aaa73e45efda880266a2d6e26b84ef5']

builddependencies = [
    ('binutils', '2.40'),
    #('Autotools', '20220317'), use system version
        
]

preconfigopts = 'autoreconf -i -f &&'

sanity_check_paths = {
        'files': ['lib/libcares.a', 'lib/libcares.%s' % SHLIB_EXT, 'lib/pkgconfig/libcares.pc'],
            'dirs': ['include', 'share/man'],
            
}

moduleclass = 'lib'
